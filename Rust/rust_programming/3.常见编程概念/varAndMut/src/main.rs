const MAX_POINTS: u32 = 100_000;

fn main() {
    let mut x = 6;
    println!("value x is {}", x);
    x = 5;
    println!("value x is {}", x);
    println!("Hello, world!, {}", MAX_POINTS);

    let spaces = "  ";
    println!("str len is {}", spaces.len());
}
